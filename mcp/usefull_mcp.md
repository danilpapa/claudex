# Usefull mcp's

* Context7 - mcp for searching libraries. Allows to searching and reading documentation
* PostgresMCP - connection to a DB in read-only mode. Allows to flirt with your DB
* TestSprite - complex testing tool 
* Supabase - adapter over Postgres